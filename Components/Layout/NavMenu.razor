@inject NavigationManager NavigationManager
@implements IDisposable 
<nav style="background-color: #386641; display: flex; justify-content: space-between; width: 100%">
    <div>
        <a class="navbar-brand" , href="/">Dinesaur</a>
        <a href="#ourhistory">About Us</a>
    </div>
    <AuthorizeView>
        <Authorized>
    <div>

        <a href="/Account/Manage"> @context.User.Identity?.Name</a>
    </div>
            <div class="nav-item">
                    <form action="Account/Logout" method="post">
                    <AntiforgeryToken />
                    <input type="hidden" name="ReturnUrl" value="@currentUrl" />
                    <button type="submit" class="nav-link">
                          <span aria-hidden="true"></span> Log Out
                    </button>
                </form>
    </div>
    </Authorized>
   <NotAuthorized>
            <div class="dropdown">
                <a href="">Sign Up</a>
    <div class="dropdown-content">
        <a  href="/Account/Register" style="color:black;"> Sign Up as a Customer</a>
                    <a href="/Account/RegisterRStaff" style="color:black;"> Sign Up as a Retaurant Staff</a>
    </div>

    
     

   </div>
   <div>
        <a href="/Account/login">Login</a>
    </div>
    </NotAuthorized>
    </AuthorizeView>
</nav>

@code {
    private string? currentUrl;
    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }

}